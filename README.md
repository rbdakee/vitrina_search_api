# Search Vitrina API

REST API для поиска и фильтрации объектов недвижимости на базе FastAPI и PostgreSQL.

## 🚀 Технологии

- **FastAPI** - современный веб-фреймворк для Python
- **PostgreSQL** - реляционная база данных
- **SQLAlchemy 2.0** - async ORM для работы с БД
- **Pydantic** - валидация данных и автоматическая генерация документации
- **asyncpg** - асинхронный драйвер для PostgreSQL
- **Swagger/OpenAPI** - автоматическая документация API

## 📋 Требования

- Python 3.11+ (рекомендуется 3.11)
- PostgreSQL 12+
- pip или poetry для управления зависимостями

## 🔧 Установка

1. Клонируйте репозиторий или создайте проект

2. Убедитесь, что у вас установлен Python 3.11:
```bash
python --version
# Должно быть: Python 3.11.x
```

3. Создайте виртуальное окружение:
```bash
python -m venv venv
# или для Python 3.11 явно:
python3.11 -m venv venv
```

4. Активируйте виртуальное окружение:
```bash
# Windows
venv\Scripts\activate

# Linux/Mac
source venv/bin/activate
```

5. Установите зависимости:
```bash
pip install -r requirements.txt
```

6. Создайте файл `.env` на основе `.env.example`:
```bash
cp .env.example .env
```

7. Заполните `.env` файл реальными данными подключения к PostgreSQL:
```env
DATABASE_URL=postgresql://user:password@localhost:5432/vitrina_db
```

8. Убедитесь, что база данных создана и таблица `properties` существует (см. SQL схему в описании задачи)

## 🏃 Запуск

### Вариант 1: Локальный запуск

Запустите приложение с помощью uvicorn:

```bash
uvicorn main:app --reload --host 0.0.0.0 --port 8000
```

Приложение будет доступно по адресу: http://localhost:8000

### Вариант 2: Docker

#### Сборка и запуск контейнера:

```bash
# Сборка образа
docker build -t search-vitrina-api .

# Запуск контейнера
docker run -d \
  --name search_vitrina_api \
  -p 8000:8000 \
  --env-file .env \
  search-vitrina-api
```

#### Использование Docker Compose:

```bash
# Запуск
docker-compose up -d

# Просмотр логов
docker-compose logs -f

# Остановка
docker-compose down

# Пересборка после изменений
docker-compose up -d --build
```

Приложение будет доступно по адресу: http://localhost:8000

## 📚 Документация

После запуска приложения документация доступна по следующим адресам:

- **Swagger UI**: http://localhost:8000/docs
- **ReDoc**: http://localhost:8000/redoc
- **OpenAPI JSON**: http://localhost:8000/openapi.json

## 🔍 API Эндпоинты

### GET /api/properties/search

Поиск и фильтрация объектов недвижимости.

**Параметры запроса (все опциональны):**

- `price_min` (int): минимальная цена
- `price_max` (int): максимальная цена
- `complex` (str): название ЖК (частичное совпадение)
- `area_min` (float): минимальная площадь
- `area_max` (float): максимальная площадь
- `rooms_count_min` (int): минимальное количество комнат
- `rooms_count_max` (int): максимальное количество комнат
- `score_min` (float): минимальный рейтинг
- `address` (str): поиск по адресу (частичное совпадение)
- `limit` (int): количество результатов на странице (по умолчанию 100, максимум 1000)
- `offset` (int): смещение для пагинации (по умолчанию 0)
- `order_by` (str): поле для сортировки (category, score, contract_price, area). Добавьте `-` для сортировки по убыванию

**Сортировка по умолчанию:**
1. По категории (в алфавитном порядке по возрастанию, A-Z)
2. По рейтингу (по убыванию)
3. По цене (по возрастанию)
4. По площади (по убыванию)

**Пример запроса (URL):**
```
GET /api/properties/search?price_min=10000000&price_max=40000000&complex=Grand&area_min=40.0&area_max=100.0&rooms_count_min=1&rooms_count_max=5&score_min=4.0&address=Нура&limit=5
```

**Пример параметров запроса (JSON для справки):**
```json
{
  "price_min": 10000000,
  "price_max": 40000000,
  "complex": "Grand",
  "area_min": 40.0,
  "area_max": 100.0,
  "rooms_count_min": 1,
  "rooms_count_max": 5,
  "score_min": 4.0,
  "address": "Нура",
  "limit": 5,
  "offset": 0,
  "order_by": null
}
```

**Примечание:** Это GET запрос, параметры передаются через query string, а не в теле запроса.

**Пример ответа:**
```json
{
  "items": [
    {
      "crm_id": "139967",
      "complex": "Grand Turan Business",
      "address": "Астана, Нура, проспект Туран 41А",
      "contract_price": 38000000,
      "area": 45,
      "rooms_count": 2,
      "score": 8.4,
      "category": "A",
      "status": "Размещено"
    },
    {
      "crm_id": "142312",
      "complex": "Grand Turan Comfort",
      "address": "Астана, Нура, проспект Туран 43/1",
      "contract_price": 37000000,
      "area": 46,
      "rooms_count": 1,
      "score": 9.5,
      "category": "B",
      "status": "Размещено"
    },
    {
      "crm_id": "140476",
      "complex": "Grand Turan Comfort",
      "address": "Астана, Нура, проспект Туран 43/1",
      "contract_price": 38500000,
      "area": 46,
      "rooms_count": 1,
      "score": 9.5,
      "category": "B",
      "status": "Размещено"
    },
    {
      "crm_id": "140227",
      "complex": "Grand Turan Comfort 4-1",
      "address": "Астана, Нура, проспект Туран 43/5",
      "contract_price": 39500000,
      "area": 42,
      "rooms_count": 1,
      "score": 9.5,
      "category": "B",
      "status": "Размещено"
    },
    {
      "crm_id": "137324",
      "complex": "Grand Champion",
      "address": "Астана, Нура, проспект Туран 46/6",
      "contract_price": 26000000,
      "area": 42.4,
      "rooms_count": 1,
      "score": 8.1,
      "category": "B",
      "status": "Размещено"
    }
  ],
  "total": 5,
  "limit": 100,
  "offset": 0
}
```

## 📁 Структура проекта

```
search_vitrina_api/
├── main.py                 # Точка входа приложения FastAPI
├── database.py             # Настройка подключения к БД
├── models.py               # SQLAlchemy модели
├── schemas.py              # Pydantic схемы для валидации
├── routers/
│   └── properties.py       # Роутер для работы с объектами недвижимости
├── requirements.txt        # Зависимости проекта
├── Dockerfile              # Docker образ приложения
├── docker-compose.yml      # Docker Compose конфигурация
├── .dockerignore          # Игнорируемые файлы для Docker
├── .env.example           # Пример файла с переменными окружения
├── .gitignore             # Игнорируемые файлы для Git
└── README.md              # Документация проекта
```

## 🎯 Особенности

- ✅ Асинхронная работа с базой данных (async/await)
- ✅ Полная документация Swagger с примерами
- ✅ Валидация входных данных через Pydantic
- ✅ Пагинация результатов
- ✅ Гибкая сортировка (дефолтная и ручная)
- ✅ Фильтрация по множественным параметрам
- ✅ Поддержка ILIKE поиска (регистронезависимый)
- ✅ Обработка NULL значений в сортировке
- ✅ Docker контейнеризация для легкого развертывания

## 🐳 Docker развертывание

### Требования
- Docker и Docker Compose установлены на сервере

### Быстрый старт

1. Склонируйте репозиторий на сервер
2. Создайте файл `.env` с настройками подключения к БД
3. Запустите:
```bash
docker-compose up -d
```

### Развертывание на Digital Ocean

1. Подключитесь к серверу:
```bash
ssh root@your_droplet_ip
```

2. Установите Docker и Docker Compose:
```bash
apt update
apt install -y docker.io docker-compose
systemctl start docker
systemctl enable docker
```

3. Клонируйте репозиторий:
```bash
git clone git@github.com:your_username/your_repo.git /var/www/search_vitrina_api
cd /var/www/search_vitrina_api
```

4. Создайте `.env` файл:
```bash
nano .env
# Добавьте DATABASE_URL
```

5. Запустите контейнер:
```bash
docker-compose up -d
```

6. Настройте Nginx (опционально):
```nginx
server {
    listen 80;
    server_name your_domain.com;
    
    location / {
        proxy_pass http://localhost:8000;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
    }
}
```

### Полезные команды

```bash
# Просмотр логов
docker-compose logs -f

# Перезапуск
docker-compose restart

# Остановка
docker-compose down

# Обновление кода
git pull
docker-compose up -d --build
```

## 🔒 Безопасность

- В продакшене настройте CORS для конкретных доменов в `main.py`
- Используйте переменные окружения для чувствительных данных
- Не коммитьте файл `.env` в репозиторий

## 📝 Лицензия

MIT

